# Отчет о реализации дополнительных заданий

**Общая набранная сложность:** 5 баллов.

---

### Задание 1: Новые виды фигур (Сложность: 1)
**Описание:** Придумать 3 новых вида фигур с оригинальными правилами перемещения и реализовать их классы, а также создать модификацию шахмат с новыми фигурами с минимальным вмешательством.

**Реализация в коде:**
В игру добавлен режим `"fairy"`, который заменяет стандартные фигуры на новые:
* **Чемпион (★):** ходит на 1 или 2 клетки по прямой (может перепрыгивать).
* **Колдун (✧):** ходит на 1 или 3 клетки по диагонали (может перепрыгивать).
* **Прыгун (⚶):** ходит ровно на 2 клетки в любом направлении.

**Где посмотреть в коде:**
* **Строки 40-52:** В методе `symbol` класса `Piece` добавлены новые юникод-символы для фигур `champion`, `wizard` и `jumper`.
* **Строки 72-74:** В методе `create_board` реализована логика расстановки фигур для сказочного режима (замена ферзя, слонов и коней).
* **Строки 91-97:** В методе `get_valid_moves_for_piece` прописаны векторы перемещений для новых типов фигур.

---

### Задание 5: «Откат» ходов (Сложность: 1)
**Описание:** Реализовать возможность возвращаться на ход назад вплоть до начала партии.

**Реализация в коде:**
В движке реализован стек истории `move_log`. При каждом ходе в него сохраняется снимок текущего состояния игры (доска, чей ход, счетчик ходов, флаг окончания игры и клетка для взятия на проходе). При вызове команды отката состояние восстанавливается из последнего снимка.

**Где посмотреть в коде:**
* **Строки 166-172:** В методе `make_move` создается глубокая копия доски (`copy.deepcopy(self.board)`) и параметров, которая сохраняется в `self.move_log`.
* **Строки 194-203:** Метод `undo_move` извлекает последний сохраненный стейт из `self.move_log` и восстанавливает атрибуты класса `ChessEngine`.
* **Строки 283-286:** В UI методе `play_match` обработка пользовательской команды `undo` и сброс выбранной клетки.

---

### Задание 6: Подсказка доступных ходов (Сложность: 1)
**Описание:** Реализовать функцию визуальной подсказки доступных полей для хода после выбора фигуры.

**Реализация в коде:**
Добавлен механизм интерактивного выбора фигуры (например, вводом координаты `e2`). Выбранная клетка подсвечивается пурпурным цветом, а все легальные ходы для стоящей на ней фигуры высчитываются движком и подсвечиваются зеленым фоном на выводимой в консоль доске.

**Где посмотреть в коде:**
* **Строки 233-236:** В методе `print_board_styled` формируется множество `legal_destinations`, куда записываются все возможные легальные конечные клетки для выбранной фигуры `self.selected_square`.
* **Строки 250-256:** Логика раскраски: переменная `bg` принимает значение `BG_MAGENTA` для самой фигуры и `BG_GREEN` для клеток из множества `legal_destinations`.
* **Строки 290-298:** В `play_match` обработка ввода из 2-х символов (выбор фигуры) и сохранение её в `self.selected_square`.

---

### Задание 7: Подсказка угрожаемых фигур (Сложность: 1)
**Описание:** Функция возвращает информацию о том, какие фигуры ходящего игрока находятся под боем, и визуально выделяет их на поле.

**Реализация в коде:**
Добавлена команда-переключатель `threats`. Если она активна, движок собирает все возможные ходы противника на текущей доске. Если конечная клетка хода противника совпадает с позицией вашей фигуры (включая короля), эта клетка окрашивается в красный цвет.

**Где посмотреть в коде:**
* **Строки 238-242:** В методе `print_board_styled` собирается множество `threatened_squares` путем вызова `get_all_possible_moves` для цвета оппонента.
* **Строки 253-254:** Проверка `is_threatened` — если клетка находится под ударом и на ней стоит ваша фигура, фон меняется на `BG_RED`.
* **Строки 287-289:** В `play_match` обработка команды `threats`, которая инвертирует булевый флаг `self.show_threats`.

---

### Задание 8: Сложные правила пешки (Сложность: 1)
**Описание:** Поддержка для пешки сложных правил: «взятие на проходе» и замены на другую фигуру при достижении крайней горизонтали.

**Реализация в коде:**
Движок отслеживает прыжки пешек на 2 клетки и сохраняет «битое поле» в `en_passant_target`. Если чужая пешка атакует это поле, происходит взятие с удалением проскочившей пешки. При достижении 0-й или 7-й горизонтали реализован запрос в консоли для выбора новой фигуры.

**Где посмотреть в коде:**
* **Строки 114-118:** В генераторе ходов `get_valid_moves_for_piece` добавлено условие: если целевая клетка совпадает с `self.en_passant_target`, ход разрешается.
* **Строки 138-140:** В методе `get_legal_moves` реализовано корректное удаление пешки на временной доске `temp_board` при симуляции взятия на проходе, чтобы правильно проверить, не открывается ли шах своему королю.
* **Строки 174-182:** Выполнение самого взятия на проходе в `make_move` (удаление фигуры) и установка нового `en_passant_target`, если пешка сходила на 2 клетки.
* **Строки 185-187:** Превращение пешки на уровне логики (смена `self.board[r2][c2].type = promote_to`).
* **Строки 307-314:** В UI методе `play_match` добавлен перехват хода пешки на последнюю горизонталь с запросом к пользователю (`input`), в кого он хочет её превратить.